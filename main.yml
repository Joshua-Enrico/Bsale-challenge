version: 0.0
os: linux
files:
  - source: /
    destination: /home/api/express-app
hooks:
  BeforeInstall:
    - location: scripts/befoe_install.sh
      timeout: 300
      runas: ec2-user
  ApplicationStart:
    - location: scripts/app_start.sh
      timeout: 300
      runas: ec2-user
